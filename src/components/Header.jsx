const Header = () => {
    return(
        <header>
            <nav>
                <ul>
                    <li>Catalogo</li>
                    <li>Contacto</li>
                </ul>
            </nav>
        </header>

    );
};

export default Header;